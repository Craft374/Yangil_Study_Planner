using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class rt : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Resources.Load("/prefeb/c");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
